---
const { theme } = Astro.props
const finalTheme =
  theme === "primary"
    ? "bg-primary"
    : theme === "secondary"
      ? "bg-secondary"
      : theme === "tertiary"
        ? "bg-tertiary"
        : "bg-neutral-light-1"
const classes = `border-4 border-current text-neutral-dark-1 rounded-2xl h-16 lg:h-20 px-5 lg:px-6 text-body-medium lg:text-body-big ${finalTheme}`
---

<button class={classes}><slot /></button>
